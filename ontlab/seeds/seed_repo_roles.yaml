seed_repo_roles:
  spring_petclinic:
    roles: [spring-web, domain-flow, frontend-backend-link]
  spring_data_jpa:
    roles: [spring-data, repository-patterns, persistence]
  spring_security:
    roles: [security-boundary, filter-chain, authz]
  vue_router:
    roles: [frontend-routing, component-navigation]
  axios:
    roles: [api-client, external-interface]
