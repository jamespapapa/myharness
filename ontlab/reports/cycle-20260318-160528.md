# Cycle report

## Context
- target repo: `instances/my-target-repo`
- cycle id: `20260318-160528`
- objective: unblock target-repo failure triage when eval outputs are stored as YAML
- smallest safe change: add YAML/YML support to the shared failure-loading rule used by cycle scripts
- `.codex/agents`: not present in this repository

## Failure Triage
- failure bucket summary: `missing framework rule` x2, likely pack `express-pack`
- root cause hypothesis: target eval artifacts are YAML, but the cycle scripts only parsed JSON/JSONL and crashed before bucketing failures
- evidence file path: `instances/my-target-repo/eval/failures.yaml`
- evidence symbol / route / config key / dependency name: `q1`, `q2`, `express-pack`
- why this evidence supports the claim: the target eval file already contains valid `bucket` and `pack_candidates` fields, yet both scripts failed with `JSONDecodeError` before any triage output was produced
- confidence: high
- what remains uncertain: whether future evaluators will always emit the same `failures` schema and whether YAML is the only additional format needed

## Before
- target metrics: YAML summary script `0/1` passed; YAML next-cycle planner `0/1` passed; surfaced target buckets `0`
- seed regression metrics: JSON summary script `1/1` passed; JSON next-cycle planner `1/1` passed

## Change
- changed files:
  - `scripts/failure_loader.py`
  - `scripts/summarize_eval_failures.py`
  - `scripts/plan_next_cycle.py`
  - `instances/my-target-repo/eval/failures.yaml`
  - `seeds/regression/failures.json`
  - `reports/next-cycle-brief.md`
  - `reports/seed-next-cycle-brief.md`
  - `reports/cycle-20260318-160528.md`
- rule/pack touched: evaluation failure-loading rule only
- why this should help: one shared loader now normalizes `json`, `jsonl`, `yaml`, and `yml` failure payloads, so the same target evidence can flow into summarization and next-cycle planning without touching core ontology or answer logic

## After
- target metrics: YAML summary script `1/1` passed; YAML next-cycle planner `1/1` passed; surfaced target buckets `1`; top target bucket `missing framework rule=2`; top target pack `express-pack=2`
- seed regression metrics: JSON summary script `1/1` passed; JSON next-cycle planner `1/1` passed; bucket counts preserved as `retrieval failure=1`, `should abstain=1`
- unresolved / answer failure delta: before `2` target failures were untriaged because the parser crashed; after the same `2` failures remain but are classified and actionable
- false confidence delta: none observed; this cycle only improves triage coverage and does not synthesize new answers

## Decision
- promote / defer / rollback: `defer`
- keep / rollback: `keep`
- reason: target failure classification is now measurable and seed JSON regression did not worsen, but target answer failures stayed at `2` and answer quality itself did not improve yet

## Next best change
- add the smallest `express-pack` rule that turns route registration and middleware binding evidence into answerable endpoint/security-boundary edges for `q1` and `q2`
